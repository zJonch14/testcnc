name: tmate session

on:
  workflow_dispatch:  # Permite ejecución manual
  # También puedes usar otros triggers como:
  # push:
  #   branches: [ main ]

jobs:
  debug:
    runs-on: ubuntu-latest
    
    steps:
    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
      with:
        # Configuración opcional:
        # timeout: 600  # Tiempo en segundos (10 minutos por defecto)
        # limit-access-to-actor: true  # Solo tú puedes acceder
        
    - name: Do normal workflow tasks
      run: |
        echo "Tu sesión tmate está activa"
        echo "Puedes conectarte con SSH mientras este job esté en ejecución"
        sleep 300  # Mantiene el job activo por 5 minutos
